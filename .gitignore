# Configuración local de Claude Code — específica de cada máquina/sesión,
# no es parte del código de la aplicación.
.claude/

# Cruft habitual del sistema operativo
.DS_Store
Thumbs.db

# Por si algún día se agrega tooling con dependencias
node_modules/

# IA Jugador (tools/ai-player/, Bloque 63) — credenciales locales, nunca
# van al repo. .env.example SÍ se commitea (es la plantilla sin valores
# reales de ANTHROPIC_API_KEY).
tools/ai-player/.env

# Logs de las corridas programadas (tools/ai-player/run-scheduled.js,
# Bloque 64) — texto plano local, no aporta nada versionado.
tools/ai-player/logs/
